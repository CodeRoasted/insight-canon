# Corpus registry

The authoritative index of every corpus the workspace tests or measures against. Governed by ADR-7, corpus storage &
governance — an INTERNAL CodeRoast decision record, not shipped with this repo.

**Rules (ADR-7):**
- A committed test/gate references a corpus **by the `id` below**, resolving to a **repo-local
  fixture** (e.g. the determinism `proof/corpus/`) or a **pinned + checksummed** asset — **never** an
  untracked `data/logs/` path. **Sample slices no longer live in canon git**: they live
  in `coderoast-corpora/<platform>_corpora/<corpus>/samples/` and publish to the **public hub** via
  the corpora Sample Release workflow; the canon *showcase* fetches them from the hub (a
  client-facing render, not a gate — ADR 0016 §5). What decides that a slice may publish is the
  fail-closed §2a gate `_shared/samples_safety_lint.py`, and it decides **two independent axes,
  ANDed** — never either: the **right** to redistribute (an `ATTRIBUTION.md` naming a permissive
  source, or a `SLICE.json` declaring the tree synthetic) **and** a **content** scan that refuses
  declared identifying-content classes. A licence grants redistribution and says nothing about what
  is in the bytes. The scan matches declared byte shapes one line at a time, so a pass means *no
  declared class fired* — **never** *these bytes are safe*. It is a floor, not a certificate.
- Big bytes are **not** in git (no LFS); they live in the private warehouse **`coderoast-corpora`**
  (tooling + materialized data + pinned Release assets) and materialize there via the acquisition
  recipe. Only this `data/corpora/` tree (registry + READMEs) is tracked, in canon.
- Corpora are **versioned datasets**: each materialization is an immutable `<id>/<version>` + sha256;
  the `pin` column is the current version, old versions stay reproducible.

## Big external corpora (defined here — canon-tokenizer-facing)

Each corpus is distinguished by **`id`** (the kind) and a first-class **`dialect`** (the log family
it exercises); its **`class`** carries the Rule-D storage class (public / re-acquirable /
§2a-private). New corpora land at `coderoast-corpora/<platform>_corpora/<corpus>/` (its own `scripts/`, `samples/`, and a `data/` symlink to the out-of-tree byte root `/home/windows/corpora-data/<platform>/<corpus>/<version>/`).

| id | dialect | intent / test-purpose | class | provenance · license | pin (current) | recipe | consumed by | lifecycle |
|---|---|---|---|---|---|---|---|---|
| **loghub** | system-log (BGL / Thunderbird) | real-log structural ground for cube measurement, the cross-stdlib determinism measurement, and the rich-format AMI re-measure (decision #1) | big · **re-acquirable** | Zenodo record `8196385` · **CC-BY-4.0** (attribute) | `BGL.zip` sha256 `d67fd82a711aea0157a9b83175892c6ee60e384a2ddf5bc51f39118453816da8` (57,489,019 B → `BGL.log` 4,747,963 lines); `BGL.log` + `Thunderbird_5M.log` (5 M lines) also pinned — see [loghub/README.md](loghub/README.md) | `coderoast-corpora/zenodo_corpora/loghub/scripts/download_logs.sh` (`_2k` slice + `fetch_loghub_full` → BGL + Thunderbird-5M, pin-verified) | cube gates (concluded), determinism measurement | **measurement — mostly concluded** (cube arc closed); retained for AMI #1 + determinism. See [loghub/README.md](loghub/README.md) |
| **ci-revert** | GitHub Actions | the CI-text labelled proxy: **R1** format-relative capture gate + **R2** silent-regression-on-green detection | big · **NON-re-acquirable** (90 d GHA log expiry) · **third-party/private (§2a)** | crawled public GitHub (pinned 25-repo set) · **third-party CI logs, no redistribution licence** · **PU posture** · heuristic scrub (**IPv4 redaction dropped** — `ips:0` = *not redacted*, not *none*) | `ci-revert/v1` — content-anchor sha256 `3054b158382c333301d986ad0472e2208078ee87a92edf42b33dd32a4660b059` (manifest of 8171 files); frozen `ci-revert-v1.tar.zst` 262 MB sha256 `26d3d3d6af4e461f4ab955b91346a7ce9c458469671007d33f7c63856ee8dd49`. **Private store** (Argos credential; trusted-runner fetch only — **never** a public Release, §2a) — 4132 samples / 4082 logged / 403 R1 failures / ~10 R2 positives / 19 repos / merge-green 0.807 / canon 1.5.2 | `coderoast-corpora/github_corpora/revert_corpus/scripts/ci_revert_corpus/` (retrospective crawler + `longitudinal/` collector — re-crawl **cannot** recover expired logs) | R1 gate, R2 detection — **trusted runner only** | **LIVE — v2 tooling BUILT** (`d11-ci-revert-v2`: revert-first sampling + widened recall + density selection + the longitudinal collector past the 90 d wall — the internal defect register); `v1` pinned as fallback; `v2` corpus awaits its trusted-runner crawl. Contract: ADR-8, corpus gates, oracles & measurement (internal). See [ci-revert/README.md](ci-revert/README.md) |
| **jenkins-markers** | Jenkins (Pipeline: declarative / scripted / matrix + freestyle floor) | the Jenkins **G1** marker-coverage prevalence + recognizer P/R gate for the `1.7.7` semantic package (studies/006) — real-stream stage/step skeleton per sub-type | big · **re-acquirable-with-caveat** (per-instance keep-last-N retention → **crawl→download→store one-pass**; stored bytes permanent) · **third-party/private (§2a)** · **scrub stricter than ci-revert** (workspace paths / node names / SHAs / `[EnvInject]`) | REST-API downloads from robot-allowed + token-authed **public Jenkins, multi-instance** (federated → per-instance-versioned; mono-instance = endogamy) · third-party build logs, **no redistribution licence** | `jenkins-markers/v1` — content-anchor sha256 `410b924162583713ed4236a81ac158ee59af0d2c7d83725a9dcfab462d49ac45` (115-file manifest = 113 logs + `corpus.jsonl` + `manifest.json`, ~13 MB); **113 logs / 7 instances** — matrix 36 (3 inst) · declarative 39 (7) · scripted 11 (4) · freestyle 27 (6); R1 `result` SUCCESS 72 / FAILURE 28 / ABORTED 9 / UNSTABLE 4; leak-scan CLEAN. **v2 (2026-07-10, PINNED — Eqya ratification on the Kleio G1 re-measure)** — anchor `ae5f19f144bb7476022abfb003c77e23cd0d5220785a11b2ebcccac76edce18c` (228-file manifest = 113 logs + 113 `.meta.json` ground-truth sidecars + `corpus.jsonl` + `manifest.json`, 43 MB); SAME 113 builds, capture-complete (true head+tail, no read ceiling + full wfapi trees 67 trees / 2145 steps); leak-scan CLEAN (226 artifacts) | `coderoast-corpora/jenkins_corpora/marker_corpus/scripts/` (`jenkins_supply_probe.py` recon + `jenkins_fetch.py` capture + `build_corpus.py`/`extend_corpus.py`/`scrub.py`/`leak_scan.py`) — **Argos credential / trusted runner** | **ASSEMBLED + FROZEN — v1 (2026-07-09) · v2 (2026-07-10)**. **v1** counts in `ASSEMBLY-v1.manifest.json` → Eqya-ratified → Kleio G1 PASS scoped to Declarative Pipeline (studies/006) — **superseded 2026-07-10**, retained as the historical anchor (`410b924…`; its G1 PASS graduated to the v2 re-grounding). **v2** (`ASSEMBLY-v2.manifest.json`) closes the studies/006 capture follow-ups (Argos): the 1.5 MB read-ceiling truncation is gone (**14/14** giants recovered their `Finished:` epilogue) + the **full wfapi tree** (stages+steps) is stored per build + a `.meta.json` ground-truth sidecar (API `result` outcome anchor + tree); **92/92** whole-logs byte-identical to v1 (SAME sample). → Kleio G1 **RE-PASS on v2** (studies/006 § v2 re-grounding: the step caveat closed with step recall **100%** on the claim carrier, outcome **100%** on depth logs, UNSTABLE 4/4, no claim regression; the new middle-elision confound opposite-signed + segregated + confined to the corroborating-only matrix leg) → **Eqya RATIFIED 2026-07-10 — pin FLIPPED: v2 (`ae5f19f1…`) is the gate-of-record**; claim scope unchanged (Declarative Pipeline). Frozen via `_shared/release_corpus.sh freeze` (**option-A**: excludes the assembly self-`manifest.sha256`); ships as the **private Release asset `jenkins-markers-v{1,2}`** on `coderoast-corpora` (`release_corpus.sh propose`, §2a-guarded). Scope: ADR-7, corpus storage & governance (internal) |
| **gitlab-markers-probe** | GitLab CI (`gitlab-ci`; `section_start/end` markers + the `Running with gitlab-runner <ver>` producer key) | studies/012 trace-grain **depth probe** — the free-token **Gate-0** proof (a free-tier `read_api` PAT unlocks `/jobs/:id/trace`, **51× 200** where unauth was 0/7) + the P2–P6 marker-skeleton gates on real gitlab.com traces. **Existence + grounding, NOT prevalence** (v1 single-host; v2 adds the multi-host **server axis** — cross-version robustness across two older self-hosted GitLab servers, still existence/grounding, never prevalence) | mid · **NON-re-acquirable** (gitlab.com job-log expiry) · **third-party/private (§2a)** · scrub **jenkins-markers grade** (section markers + JSON oracle survive byte-identical, PII payload redacted; `leak_scan` gates `.log` + `.json`) | crawled public **gitlab.com** via a free-tier `read_api` PAT (**Argos credential**, trusted-runner fetch only) · **third-party CI logs, no redistribution licence** · §2a-private | **v1 (token arm, gitlab.com single-host)** — content-anchor pin `c8742cb461eb95a701c5dda9b1f4c6c9e4b42d27b3ec9f05eefad1b729d63654` (manifest of 111 files); asset `gitlab-markers-probe-v1.tar.zst` 1.1 MB sha256 `f7d176a7cc598070f207c59d9d6777597a07d9877fecf97d7a7d01ad692d2bd1` · **51 traces / gitlab.com** · Kleio trace-grain (frozen instrument `ad93c84e`, scored on the RAW capture): P2 100% present+paired · P3 100% envelope/span · P6 100% fetch · **P4 87.5%** (recognizer `\r`-anchor limit; API authoritative) · P5 runner-vocab safe — **all reproduce on the asset** (`g0(asset)==g0(raw)`; see the CR-fidelity correction in lifecycle). **v2 (server axis, 3 hosts / 11 projects / 83 traces)** — pin `ee83ab1e78e1d3c5c128f51dfceb3c2bcb10fb5805b4525f4c4c86cdbed60232` (manifest of 186 files); asset `gitlab-markers-probe-v2.tar.zst` 2.2 MB sha256 `059df82fcf37f758ec23de0d56242159e30221f91d832d8eea196d10e6297c9e` · gitlab.com 71 / freedesktop 4 / KDE 8 = **85.5% gitlab.com → anti-endogamy bar UNMET → PROBE-grade (existence + cross-version, NEVER prevalence)**. **Both private store — never a public Release (§2a).** | `coderoast-corpora/gitlab_corpora/marker_probe/scripts/` (`gitlab_fetch_probe.py` capture → `scrub.py` [byte-faithful: surrogateescape, preserves bare-CR marker delimiters] → `finalize_manifest.py` → `leak_scan.py` gate; freeze via `_shared/release_corpus.sh freeze`) — re-crawl **cannot** recover expired logs | studies/012 P2–P6 (Kleio); the Gate-0 free-token proof | **FROZEN — v1 (token arm, 2026-07-23; asset CORRECTED 2026-07-23) · v2 (server axis, 2026-07-23).** Phase-0 `v0` = unauth oracle-only (`dc91e16`, not frozen — traces 401'd). **CR-fidelity correction (whitehat, pre-publication):** the first v1 freeze scrubbed via `Path.read_text()`, which fold­ed the bare-CR marker-clear delimiters (`\r\x1b[0K`, on every runner) to `\n` and thereby spuriously satisfied the scorer's `^`-anchored outcome recognizer — the defective asset scored **P4 100%** where the raw capture scores **87.5%** (a fabricated gate pass). `scrub.py` now decodes byte-faithfully (surrogateescape, no io-layer newline translation) and its marker-KEEP mirrors the frozen scorer's `PFX` (stream-tag + any-CSI); the corrected v1 asset reproduces the raw capture on every gate. The defective pin `d58a5827…`/asset `24dcb50a…` are **RETIRED** (never published off-machine — the §2a Release was still Founder-gated). v2 adds the multi-host server axis but stays PROBE-grade: the Phase-1 anti-endogamy bar (≥3 hosts, ≤60% gitlab.com, ≥8 projects) is **UNMET at 85.5% gitlab.com** — the bar-meeting bulk-pull `gitlab-markers` (`marker_corpus`) unit LANDED 2026-07-26 — 6 hosts, gitlab.com 21%, bar MET, outcome A (see its row above); this probe unit stays PROBE-grade (Eqya RULED 2026-07-23: proceed to Phase 1; existence/grounding claimable now, prevalence/depth/`step_duration` forbidden until the bar is met on the corpus). Spec: ADR-7, corpus storage & governance (internal) |
| **gitlab-markers** | GitLab CI (`gitlab-ci`; `section_start/end` markers + the `Running with gitlab-runner <ver>` producer key) | studies/012 **Phase-1 bar-meeting corpus** — the anti-endogamy corpus the GitLab **depth claim rests on** (bars 1-7 scored on it). Deliberately a SEPARATE unit from `gitlab-markers-probe`, which stays PROBE-grade (existence + grounding + cross-version, **never** prevalence): this is the one that MEETS the anti-endogamy bar | big · **NON-re-acquirable** (GitLab job-log expiry — measured, not assumed: 2 of 627 traces expired within a day at the bar-7 re-fetch) · **third-party/private (§2a)** · scrub jenkins-markers grade + byte-faithful (surrogateescape; section markers survive **identical raw↔scrubbed on 627/627 traces**, PII payload redacted — 259k IPs / 6 354 emails / 588 secrets / 19 934 SHAs; `leak_scan` gates `.log` + `.json` + `.jsonl`, CLEAN over 1 381 artifacts) | crawled public GitLab across **6 hosts** — gitlab.com, gitlab.freedesktop.org, invent.kde.org, gitlab.gnome.org, gitlab.haskell.org, code.videolan.org — via per-host free-tier `read_api` PATs (**Argos credentials, per-host isolated: a host's token is never sent to another host**) · **third-party CI logs, no redistribution licence** · §2a-private | **v1 (bar-meeting corpus)** — content-anchor pin `1006df2617230b82efc7fef653172d29a49a24cfd26d58e278bc40a4aeaaaa0f` (manifest of 1 382 files); asset `gitlab-markers-v1.tar.zst` 31 MB (from 599 MB) sha256 `1fbcf723efadc59f6137b67bb412fabd8532855372c561669918cb3c566f1522` · **627 traces / 33 projects / 6 hosts, gitlab.com 21% of projects** (the probe unit's v2 was 85.5% → bar UNMET; this is what the pull existed to fix) · Kleio Phase-1 score (frozen instrument, `--phase1`; **two legs split at runner 18.9**, boundary fixed pre-numbers — the per-line-timestamp default flip is measured to sit in **(18.5, 18.8]**, so 18.9 is a boundary *after* the flip, which makes the modern leg conservative and the old leg a mixed population): **MODERN (≥18.9, 430 ran-job) CARRIES** — bar2 anti-endogamy MET (6 hosts, top host 20.7% ≤60%, 28 projects, top project 5.6% ≤25%) · bar3 coverage **100%** · bar4 envelope **99.5%** / span **98.2%** · bar5 outcome **100%** · bar6 proof pair **CLOSED** (dual-implementation byte-identical) · bar7 capture invariance **625/627 survived, 100% skeleton agreement**; **OLD (<18.9) does NOT corroborate — mixed population, uninterpreted** (37 of 174 are stamped; contamination flatters envelope and penalises span, so neither bar is attributable to old-generation traces) (bar4 envelope 93.7% FAIL — the expected weaker grounding on old runners, scatter with no adjudicable per-host clock-skew). **Eqya ruling: OUTCOME A** — the per-dialect depth claim is EARNED on the modern leg; it reaches the external surface only when the GitLab semantic package ships | `coderoast-corpora/gitlab_corpora/marker_probe/scripts/` — **shared with the probe unit, deliberately not duplicated**: `gitlab_phase1_pull.py` (bulk pull; polite 0.5 s/req, per-host abort, Retry-After, bounded budget) → `scrub.py` → `finalize_manifest.py` → `leak_scan.py` (gate) → `g0_gitlab.py --phase1` (score) → `gitlab_capture_invariance.py` (bar 7). Freeze: `_shared/release_corpus.sh freeze gitlab_corpora/marker_corpus`. Verify: check the asset against `.tar.zst.sha256`, extract, then `sha256sum -c gitlab-markers-v1.manifest.sha256` **from the extracted corpus root** — re-crawl **cannot** recover expired logs | studies/012 Phase-1 bars 1-7 (Kleio); the sanctioned GitLab semantic package build (homing → Kleio, build → Daidalos+Heph, release/CI wiring → Argos) | **FROZEN — v1 (bar-meeting corpus, 2026-07-26).** Frozen tree verified by extracting the asset and checking all 1 382 files against the content-anchor manifest — byte-for-byte OK. **Not yet released: the §2a-private release proposal is the Founder's trigger** (`release_corpus.sh propose gitlab_corpora/marker_corpus v1`). The pin above is the sha256 **of the freeze's content manifest**, which is NOT the sha256 of the in-tree `PROBE-v1.manifest.json` (`e7669119…`, the population enumeration the scorer reads) — two different anchors, do not substitute one for the other. **§2a-private — never a public Release.** Declared residuals carried by the claim: the mature-OSS roster bias (median/long-tail producers unmeasured, a Phase-1.x sampling owed) and the wireshark unexpanded-`%s` malformed markers (structure-only, no derivable duration) |

| **gcc-buildlog** | gcc/make (C/C++ compiler build logs) | **studies/007** gcc/make build-log *depth* oracle for the `1.7.8` compiler-dialect semantic package — the console recognizer scored against three machine oracles (`compile_commands.json` + per-TU `-fdiagnostics-format=json` + a normalized `build_graph.json`) since gcc/make has **no wfapi**; three axes: **self-build-diff** (gcc 15.2-vs-15.3 bootstrap — the volume flex 2.a), **toolchain-upgrade** (same project, 15.2-vs-15.3 — 2.b), **flag-drift** depth pairs (baseline vs strict warnings) | big · **re-acquirable** (regenerable from the pinned recipe) · **MIXED class**: gcc self-build **GPL-3.0 public-eligible** (Corresponding Source shipped via malf-toolchain) · external projects + depth corpus **§2a-private** (build logs echo third-party source in diagnostics) | gcc **15.2.0/15.3.0/16.1.0** built from **GPG-verified** ftp.gnu.org source (GPL-3.0, unmodified) on one box / one host compiler; externals fmt·spdlog (MIT), tinyxml2·zlib (Zlib), **LLVM libLLVMSupport** (Apache-2.0-w/-LLVM-exception — clang's source compiled **WITH gcc**, not using clang) · posture `coderoast-corpora/gcc_corpora/buildlog_corpus/LICENSE_POSTURE.md` | `gcc-buildlog/v2` — **manifest sha256 (registry PIN)** `5bf2ef9eb4e1f5b1f67fdd3e32e40831cc1ae441bc9895002f14b7e1eebbb5c3`; asset `gcc-buildlog-v2.tar.zst` 1.6 MB sha256 `3f148caebd513dd59dc55ba76059b44f84ba78a159fc57cd8bbe8948009b8baf`; **9 pairs / 111 files / 118 MB**; content-anchor (`manifest.json`) `313ab929…`. (v1 `b6686128…` / 7 pairs retained as the historical anchor.) Pairs: self-build 15.2-vs-15.3 + **15.3-vs-16.1** (major gap → real drift `-Wdelete-non-virtual-dtor`) · **ourstack canon 15.3-vs-16.1** (C++23-**modules**, our own Apache-2.0 code, 108 TUs) · depth zlib (`-Wconversion`×29/`-Wsign-conversion`×8) · depth tinyxml2 (`-Wsign-conversion`×19/`-Wold-style-cast`×5) · depth fmt/spdlog (clean → structural) · upgrade fmt · upgrade LLVMSupport (150 TUs, ~10k diag/arm). Build systems: gcc-bootstrap · cmake+ninja · cmake+make · make · **c++23-modules** | `coderoast-corpora/gcc_corpora/buildlog_corpus/scripts/` (`capture_gcc_selfbuild.sh` bootstrap + `assemble_corpus.sh` + `llvm_build.sh` + `gcc_capture.py` sidecars + `write_manifest.py`); **scorer/contract = `gcc_oracle.py`** (Kleio) — **Argos credential / trusted runner for private pairs** | **ASSEMBLED + FROZEN — v2 (2026-07-11, `1.7.8` prep, Argos)** (v1 superseded, retained as historical). v2 adds the gcc-16.1 pairs: **canon builds CLEAN under gcc-16.1** (223/223, no ICE, zero warnings — 16.1-viable for our core; the LogCraft ICE that benched 16.1 does NOT hit canon), and the 15.3→16.1 self-build carries real **cross-major drift** (the wider gap the 15.2→15.3 patch lacked). gcc-16 **dropped `-fdiagnostics-format=json`** → SARIF, normalized in the capture layer (gated by `diag_schema_parity.sh` — the scorer is unchanged). Depth signal in the flag-drift pairs (zlib/tinyxml2); self-build = the volume/determinism exhibit; the toolchain-upgrade pairs (fmt/LLVM/canon) are warning-clean (empty delta — honest "nothing silently changed"). Frozen via `_shared/freeze_corpus.sh`; ships as the **private Release asset `gcc-buildlog-v2`** (`release_corpus.sh propose`, §2a-guarded). |

## Repo-local fixtures (defined where their tests own them — listed for completeness)

These are **small permanent fixtures**, committed in git lock-step with their tests (ADR 0016 §2).
Do not relocate them here.

| id | what | home | consumed by |
|---|---|---|---|
| **canon-goldens** | determinism golden-hash corpus (byte-identity oracle) | `insight-canon/` tests + `insight-canon/proof/` | the F5 cross-stdlib determinism gate |
| **eidos-fuzz** | parse-path fuzz replay set (minimized) + curated seeds | `insight-eidos/fuzz/corpus/` | `parse_fuzzer` (libFuzzer/ASAN gate) |
| **playground-red** | cube REDs + e2e scenario fixtures (1:1 scenario↔test) | `coderoast-server/insight-playground/` | the e2e regression suite |

## Published sample slices (ADR 0016 §5 — home moved to the warehouse + hub)

Each big corpus's small, deterministic **published slice** lives at
`coderoast-corpora/<platform>_corpora/<corpus>/samples/` — **not** in canon git. Its purpose is now a
**client-facing showcase** (canon's `det_proof` run over it, published as evidence — the
[Samples Showcase](../../../.github/workflows/samples-showcase.yaml) workflow fetches from the hub),
**not** a zero-fetch canon test gate: the determinism gate uses its own repo-local `proof/corpus/`
fixture, and the end-to-end coverage is owned by Eidos + the playground e2e. So no canon test needs an
in-git slice, and the slices were removed from canon (the CC-BY LogHub `_2k` set + the ci-revert
synthetic fixture).

**A published slice MUST clear both §2a axes (§2a).** For a **first-party / redistributable**
corpus (LogHub CC-BY-4.0) the slice is real bytes + an `ATTRIBUTION.md`; for a **third-party / private**
corpus (`ci-revert`, `jenkins-markers`) it is a **synthetic shape-fixture** (`SLICE.json "synthetic":
true`) — never real crawled logs; the real-data gates run private on a trusted runner. Two lints
enforce it, fail-closed, before anything is published: the warehouse
`coderoast-corpora/_shared/samples_safety_lint.py` (RIGHT **and** CONTENT, per the Rules above) and
the workspace [`corpus_registry_lint.py`](../../../scripts/corpus_registry_lint.py) Rule D (no
third-party-class corpus has committed bytes in canon's public tree).

> **The phrase "public-safe by construction" is retired and must not come back.** It stated a
> verdict the instrument cannot reach, and it is what carried a real third-party operational corpus
> onto the public hub and into 16 published release tarballs — a licence-only gate reading as a
> content guarantee. The gate's own header now names its bound; this page must not restate it more
> confidently than the gate does. Attribution + a declared class list is what a published
> third-party slice is carried on: **declared, not asserted clean.**
